# OPEN_QUESTIONS — sprint-2 (2026-09-05) — for the owner; nothing here was assumed closed

1. **DEC-24..26 (Architecture owner).** The alias-law text is drafted in RESEARCH-1.2 D-2 (RG), HARDEN-2.2 D-2/D-4 (CC, W) and MET-2.2 §5 (all three + doc_id supersession + R25 label). One ruling closes all three; a MET-2.3 row records it.
2. **Operations owner.** DEPLOY-1.1 DR-4 and `TASK-REG-014` name an operations owner; MET-2.2 §1 does not. Name one (any of the four accounts) or fold the role into the security owner — one row in MET-2.3.
3. **Manifest owner and corpus owner.** 13 + 46 HARDEN-1.1 cells still read `[NEEDS DEFINITION]`. The manifest owner is, in practice, the account that appends amendments (Kenny-bytes); saying so in MET-2.3 resolves 13 cells. The corpus owner is a 03_ question and is not this repository's to decide.
4. **DEPLOY-1.2 values.** RTO, RPO and the L5 multi-region drill protocol (DEC-23 values) — the infrastructure owner (Ken-nough) supplies numbers; the delta is then a ten-minute write.
5. **cdss-compiler prefix.** DEC-09 ratified four prefixes; the compiler repository has none (REPO-MAP v3 `[PENDING-ENUMERATION]`). Mint `CP` or rule that it lives under `SPINE`/`E`.
6. **02_ derived-artifact regeneration.** DEC-01 is closed; IMAGO-3 v4 and the v4 page are regenerated here. The `cdss_complete_stack.md` / `cdss_diagrams.html` successors (HARDEN-1 rows 41–42) remain queued: say whether sprint-3 does them or the corpus/architecture owner does.
7. **Vendored skill pack in the ledger.** 118 of the 146 new ledger rows are the Impeccable skill pack and agent definitions (`.claude/**`, `.github/skills/**`, `.github/agents/**`). They are enumerated because MT2 §3 says every artifact gets a row; their hardening is by version pin. If the owner prefers to exclude vendored tooling from R29 scope, that is an amendment to the HARDEN-1.1 scope rule, not a deletion of rows.
8. **Impeccable detector on the v4 page.** CI runs the detector over changed HTML pages; the v4 page keeps the v3 look via tokens with the v3 values as fallbacks. If the detector reports drift, its output is pasted and the page is fixed on the branch before merge.
