# cdss-compiler (Proposed — DEC-09; skeleton only)
Guideline Compiler (EN-3): narrative → computable → GenericArgument. ADOPT per MAK-ELSM: cqframework CQL translator + clinical-reasoning (deploy hapi-fhir-jpaserver-starter upstream, NOT legacy cqf-ruler) + WHO SMART IGs as source content. The build is the one layer nobody ships: the lift from PlanDefinition/CQL into GenericArgument templates with per-warrant evidence-tier backing.
Layout: `sources/` (pinned guideline artifacts) · `lift/` (annotation layer) · `bundles/` (versioned GenericArgument outputs → registry PR gateway) · `assist/` (K compiler-assist point, Proposed: ArgEval/ArgTumour pattern, proposer-with-verifier, prompt-carded) · `tests/` (round-trip: compiled warrant executes identically under the evaluator; FML hook dormant pending DEC-05/FZ-2).
