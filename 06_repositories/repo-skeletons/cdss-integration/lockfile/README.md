# Integration lockfile
Pin-set = version stamp in every trace and every register entry. Rollback = redeploy prior pin-set (DEPLOY-1); append-only ledgers never roll back — corrections supersede (SPINE-4).
