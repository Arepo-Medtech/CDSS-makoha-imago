# ci/pipeline.yml — STUB (Proposed; not runnable; wired for real in cdss-evalstack imports)
# Tier 1+2 shape per Arch §11.1; every repo imports pipeline definitions from cdss-evalstack (I). Added 2026-09-05 (sprint-1, survey-2 BSQ-0392); skeleton file, no code claimed.
# stages:
  #   - pin: consume cdss-spine@[PIN]; fail on contract drift (spine PR breaks consumers visibly)
  #   - eval-refusal: loaders MUST refuse EVAL/DEV-tagged material (proven here; Annex H-1 §10) — a passing load of an EVAL tag is a build failure
  #   - governance: admissibility validator (cdss-governance shared CI action) — no card, no promotion; learners register before first training run
  #   - supply-chain: Trivy scan, ECR sign, signed SBOM (Syft/CycloneDX) -> R3
  #   - r29-ratchet: row-completeness check on instruction-bearing artifacts (ACTIVATES on DEC-02 ratification; MT2 §7(4))
