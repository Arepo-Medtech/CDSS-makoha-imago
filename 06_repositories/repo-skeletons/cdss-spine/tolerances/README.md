# Metric tolerances — versioned configuration
I8 is the authoritative copy of every tolerance number and says so (Arch §8). This directory carries them as config consumed by cdss-evalstack pipelines; a change is a Primer-I change-class event.
