<!-- SKELETON — Proposed (DEC-09; Arch §10 mirrored). No code, build, or deployment is claimed. Every directory README states what its owning primer/annex requires; content lands only through that repo's own gauntlet. -->
# cdss-engine (Existing — Primer A; annex A10)
Stateless compute container + trace emitter; **no clinical numbers of its own** — a calculator over cdss-library releases. This pass adds the argument-payload envelope: LR chain = warrant instantiation, posteriors = qualifier raw material, red-flag overrides = safety-class claims (A10).
Layout: `service/` · `trace/` (A8 schema consumed from spine) · `properties/` (A8 first properties; runs land in R25) · `tests/` (byte-identical replay = L1 exit; SPINE-2 payload refusal fixtures).
Sourcing (MAK-ELSM §03): BUILD; pgmpy-class primitives; Babylon code STUDY ONLY (patent-encumbered).
