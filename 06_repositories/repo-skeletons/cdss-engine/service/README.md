# Engine service (stub)
Stateless; consumes coded findings (grounds w/ provenance) + library pin; emits posterior + trace + warrant-instantiation payload (A10 steps 1–7). Abstention is a legal output.
