<!-- SKELETON — Proposed (DEC-09; Arch §10 mirrored). No code, build, or deployment is claimed. Every directory README states what its owning primer/annex requires; content lands only through that repo's own gauntlet. -->
# cdss-graph (Existing — Primer E; annex E10)
Deterministic graph builds (hashable); rebuild = f(registry version). This pass promotes prunes to **rebuttal records** and co-applicable conflicts to **DetectedIssue** (SPINE-6 — surface, never merge).
Layout: `build/` (deterministic rebuild + hash) · `traversal/` (worked traversals per E8) · `tests/` (hash-equality determinism = L3 exit; pluralism test = DEPLOY-2 §8).
Substrate: Aurora PostgreSQL at L3 scale, Neptune when justified (Arch §11.4).
