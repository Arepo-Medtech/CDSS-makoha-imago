# Acceptance
1. Rebuild determinism: same registry pin -> identical build hash. 2. Prune -> rebuttal record emitted. 3. Two conflicting applicable GenericArguments -> DetectedIssue on both, no silent rank/merge/suppress.
