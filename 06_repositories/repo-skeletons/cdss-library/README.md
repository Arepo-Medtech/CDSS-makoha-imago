<!-- SKELETON — Proposed (DEC-09; Arch §10 mirrored). No code, build, or deployment is claimed. Every directory README states what its owning primer/annex requires; content lands only through that repo's own gauntlet. -->
# cdss-library (Existing — Primer B; annex B10)
Versioned data releases + validator; **answers to sources, never scores**. Rows are argument **backing** (tier E1/E2/E3 + source + currency). Boundary: fuzzy membership functions are NOT library rows (FZ-2 dormant — FML artifacts travel the compiler path if ever ratified).
Layout: `rows/` (per-domain; adult respiratory = L1 exemplar) · `sources/` (R6 source registry surface) · `validator/` (B8 invariants; captured output required) · `releases/` (versioned + manifest).
