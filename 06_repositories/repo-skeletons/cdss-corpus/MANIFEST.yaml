# MANIFEST stub — cdss-corpus (Skeleton, Proposed; format per Arch §10 / Harness manifest discipline)
name: cdss-corpus
status: skeleton-proposed        # nothing emitted yet; this file becomes the artifact manifest on first emit
emits: [checkpoint aggregates only]
consumes:
  cdss-spine: "[PIN NEEDS DEFINITION]"   # contracts consumed as pinned dependency; a spine change visibly breaks this repo's CI
registers_written: [R9 coverage map, R21 exposure ledger, R28 aggregate mirror]
version_stamp: "[joins the integration lockfile pin-set — register law §12.1(4)]"
account_boundary: own AWS account — this manifest is the only dev-visible surface
