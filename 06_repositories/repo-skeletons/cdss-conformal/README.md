<!-- SKELETON — Proposed (DEC-09; Arch §10 mirrored). No code, build, or deployment is claimed. Every directory README states what its owning primer/annex requires; content lands only through that repo's own gauntlet. -->
# cdss-conformal (Existing — Primer F; annex F10)
Wrapper library + calibration reports; **pure math, no data retained**. The conformal set IS the mandatory qualifier (SPINE-2). Type separation per FZ-1 (dormant): membership degrees never render as probability.
Layout: `lib/` (MAPIE-based per MAK-ELSM ADOPT; BSD-3) · `calibration/` (exchangeability protocol, stratum minimums per F8; slices external to training) · `reports/` (coverage vs I8 tolerance — dossier feed).
