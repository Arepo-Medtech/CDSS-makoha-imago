<!-- SKELETON — Proposed (DEC-09; Arch §10 mirrored). No code, build, or deployment is claimed. Every directory README states what its owning primer/annex requires; content lands only through that repo's own gauntlet. -->
# cdss-corruption (Existing — Primer G; annex G10)
Suites as data + the rulebook (clinician-reviewable in isolation; G8's 18 starter rows want the red pen). This pass proposes the **ARG-class** (missing qualifier / empty rebuttal with findings / render-reweight — 100% catch before fabric v1) and the **FZ-5 boundary-sweep class (DORMANT until DEC-05)**. Findings publish as rebuttals every face can see (EN-5).
Layout: `rulebook/` (18 rows + proposed ARG rows; R8) · `suites/` (generated, label-guaranteed) · `reports/` (catch rates; incident pairing per R20 law).
Kill criterion stands: catch <100% twice consecutively halts the release train (Arch §13.5).
