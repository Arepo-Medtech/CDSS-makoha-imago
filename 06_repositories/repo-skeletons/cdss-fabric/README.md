# cdss-fabric (Proposed — DEC-09; skeleton only, no code claimed)
Justification-fabric service: GAAM Generic/Actual/Deviation semantics, deterministic evaluation of completed argument trees (wrapping the existing release-spine checks), compliance projector, append-only hash-chained ledger (substrate per DEC-04).
Layout (proposed): `service/` (evaluator wrap; consumes cdss-spine@vX contracts) · `projector/` (auditor-face read model, register-scoped projections — SPINE-9: derived, disposable, rebuildable) · `deviation/` (composer + taxonomy loader) · `ledger/` (hash-chain over Aurora pattern; S3 object-lock anchor) · `tests/` (fabric replay; SPINE-2 refusal; CONTRACT-RRI-1 invariance; ARG-class corruption fixtures).
Sourcing (MAK-ELSM): BUILD — no public precedent; TweetyProject (LGPL) as solver candidate, Carneades-4 as design document (dormant since 2017 — mine, don't depend), argumentative-llms as LLM-bridge reference; de-risk via Stranieri collaboration. Schemas live in cdss-spine, never here.
